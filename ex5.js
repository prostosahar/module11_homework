// Задание 5

const x = 5;
const n = 3;

const arrow = (x, n) => {
    let result = Math.pow(x, n); //Как классно, что логика и, от части, синтаксис почти ничем не отличается от C++
    console.log(result);
};

arrow (x, n);